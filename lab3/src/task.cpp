#include "task.h"

Task::Task() : _figures(nullptr), _sz(0) {
}

// Task::Task(std::initializer_list<Figure> const & init_list) : _sz(init_list.size()) {
//     _figures = new Figure [_sz];
//     for (size_t i = 0; i < _sz; ++i) {
        
//     }
// }